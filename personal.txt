// when switching the skillLevelDropdown to a correct value (any other value basically, it should retrigerr the error check (part of the save function))

// transitions for the errors

// make a reusable error color in the theme

// put request should only be called one time and NOT in a forEach loop 

ask about should you put your code into service or controller

insert all data into DB

rename skill tab data to skillsTabData